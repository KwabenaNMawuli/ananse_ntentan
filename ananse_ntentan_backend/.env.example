# Server Configuration
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ananse_ntentan?retryWrites=true&w=majority

# Frontend
FRONTEND_URL=http://localhost:3000

# Google Gemini AI
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-3.0-pro

# Image Generation
IMAGE_PROVIDER=gemini-image
# Options: gemini-image, stability, imagen
# STABILITY_API_KEY=your_stability_api_key (if using stability provider)

# File Upload
MAX_FILE_SIZE=10485760

# Google Cloud TTS (Optional - for audio narration)
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Agentic Features (added in SDK migration)
# These are optional - defaults are used if not specified
# THINKING_LEVEL=HIGH  # Options: LOW, HIGH (default: HIGH)

